name: Microsoft Services IP-Lists

on:
  push:
    branches:
      - main
  pull_request:
    branches:    
      - main
  schedule:
    - cron: '30 5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate_lists:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Generate endpoint lists
        run: |
          mkdir -p docs/{azure,o365}
          chmod 755 ./generate_list_azure.sh ./generate_docs_index.py ./generate_list_o365.py
          ./generate_list_azure.sh
          python3 ./generate_list_o365.py
          python3 ./generate_docs_index.py

      - name: Commit generated content
        if: github.event_name != 'pull_request'
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "chore: refresh generated Microsoft endpoint lists"
          git push
